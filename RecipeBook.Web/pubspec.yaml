name: recipebook
description: Food?
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3
  dio: ^4.0.0
  flutter_svg: ^0.22.0
  google_fonts: ^2.1.0
  json_annotation:
  provider: ^5.0.0
  url_launcher: ^6.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.6
  json_serializable:
  lint: ^1.5.3

flutter:
  uses-material-design: true
  assets:
    - google_fonts/
    - assets/
    - assets/icons/
    - assets/images/